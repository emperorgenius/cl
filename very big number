#include<iostream>
#include<string>
using namespace std;

int main()
{
	string a, b;
	cin >> a;
	cin >> b;
	if(a.size() > b.size())
		cout << a;
	else if(a.size() < b.size())
		cout << b;
	else
	{
		if(a > b)
			cout << a;
		else if(a < b)
			cout << b;
	}
	return 0;
}
